{
  "name": "@tradelogx/nexus",
  "version": "0.1.0",
  "description": "TypeScript client for the TradeLogX Nexus public API (/v1)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p .",
    "prepare": "tsc -p .",
    "test": "tsc -p . && node --test test/*.test.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
