{
  "baseline_commit": "5b351e9",
  "purpose": "Freeze PR #6 real-paper behavior plus the approved native-MTF source-only delta; alpha rules and fill timing remain unchanged.",
  "approved_delta": "Replace synthesized HTF clocks with services/mtf_policy.py native-candle evidence only.",
  "sha256": {
    "services/mtf_policy.py": "f54da90f2643152f9e9864f42ea77e9fc920c4b75aba19e3190fc3e86ac199a0",
    "services/price_action_lab.py": "85b06d105e025769cffd0c2234862a6d90772ab70813b15713f3c1df15657d6d",
    "services/smc_strategy_lab.py": "009bd4f48caeb443adc9849b4708001e2414ad6119104c6916572f0169de1da1",
    "services/native_price_action.py": "5307ce702564b0739673a093d0a24478ad49ae24391f02e16fa39c0c0ae70339",
    "services/native_smc.py": "47c2b7e28fee15e3366ea5434092f13c99cb50cbf718e56a35becaf715c8c36d"
  },
  "subsequent_approved_deltas": [
    {
      "commits": [
        "829ee22 (PR #11)",
        "b7779e9 (PR #12)"
      ],
      "delta": "Unblock paper runtime hydration and execution: move native HTF loads to services/native_context_loader.py, serve lab status and live state from committed snapshots instead of request-thread provider I/O, and name the HTF_PRIMARY_UNAVAILABLE blocker explicitly.",
      "files_rehashed": [
        "services/price_action_lab.py",
        "services/smc_strategy_lab.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "verification": "services/native_price_action.py, services/native_smc.py and services/mtf_policy.py still hash to their PR #6 baseline values, so entry conditions, stop model, target R and the native MTF policy are untouched. The delta in the two lab modules is hydration, status reporting and blocker naming only; no decision or fill-timing path was modified."
    },
    {
      "commits": [
        "e824bfa"
      ],
      "delta": "Add SQLite indexes covering the one-session-newest-first reads that each lab's state() makes. Schema access paths only: no statement, threshold, rule or fill path was changed.",
      "files_rehashed": [
        "services/price_action_lab.py",
        "services/smc_strategy_lab.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "verification": "The diff in both files is additive CREATE INDEX IF NOT EXISTS statements inside the existing schema executescript, plus comments: 38 inserted lines and 0 deleted across the two. services/native_price_action.py, services/native_smc.py and services/mtf_policy.py still hash to their PR #6 baseline values. An index changes how SQLite reaches rows, never which rows it returns or in what order, and the queries themselves are byte-identical; tests/test_lab_status_reads_are_indexed.py asserts both the new access paths and that state() still answers the same shape."
    },
    {
      "commits": [
        "pending (lab state() row bounds + PA hydration honesty)"
      ],
      "delta": "Bound the per-collection reads in each lab's state() and report exact totals from COUNT(*) alongside the returned window; call state() once rather than N+2 times in the SMC metrics summary; and stop PriceActionPaperAccount.live_state presenting a freshly constructed empty engine as though it were the worker's. Read-path, aggregation and reporting only.",
      "files_rehashed": [
        "services/price_action_lab.py",
        "services/smc_strategy_lab.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "verification": "services/native_price_action.py, services/native_smc.py and services/mtf_policy.py still hash to their PR #6 baseline values, so entry conditions, stop model, target R and the native MTF policy are untouched. 91 inserted and 20 deleted lines across the two lab modules. The queries are byte-identical apart from an appended LIMIT on four PA collections (pa_candidates, pa_order_meta, pa_funding_events, pa_position_remediations) and three SMC ones (smc_order_meta, smc_candidates, smc_funding_events); their siblings pa_activity, pa_evaluations, smc_activity and the broker fills were already capped the same way. Every caller that needs a whole collection passes limit=None, and counts that were previously len() of an unbounded read now come from COUNT(*), so orders_placed and target_1_hits are unchanged. No decision, proposal, sizing or fill path was modified. Motivation was a production incident: two lab sessions left open since 2026-08-24 and 2026-09-03 made state() re-materialise whole sessions on every tick and status poll, at ~30 MB/s memory growth with a core pegged, until the container reached its 11.4 GB ceiling."
    },
    {
      "commits": [
        "pending (default operating_mode -> automatic)"
      ],
      "delta": "Default a new lab session's operating_mode to 'automatic' instead of 'signals_only', in both the config dataclass and the sessions-table column default. Requested explicitly by the operator after observing that no lab had ever placed an order. Also make execution_armed mean configured-and-able rather than the configured mode alone, so a BLOCKED lab no longer reports execution_armed alongside session_state BLOCKED.",
      "files_rehashed": [
        "services/price_action_lab.py",
        "services/smc_strategy_lab.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "verification": "A one-token change to two defaults plus comments. The three-branch dispatch on operating_mode is untouched, so what 'automatic' does -- call _place_proposal on a proposal that has already cleared every risk, attestation, persistence and feed-reliability gate ahead of it -- is unchanged; only which branch a NEW session starts on differs. Existing sessions keep their stored mode, and the read fallbacks for rows that predate the column (price_action_lab.py old.get(...) and session.get(...)) deliberately still resolve to signals_only so an old session is never silently armed. native_price_action.py, native_smc.py and mtf_policy.py still hash to their PR #6 baseline: entry conditions, stop model, target R and the native MTF policy are untouched. real_execution_allowed remains False and paper_only True in both labs, so this arms simulated fills only and cannot reach a venue."
    },
    {
      "commits": [
        "pending (first_touch_only made satisfiable)"
      ],
      "delta": "Count distinct visits to a support/resistance zone and gate first_touch_only on that instead of touch_count. Adds Zone.visit_count and Zone.inside_zone to services/native_price_action.py; no other behaviour is reached.",
      "files_rehashed": [
        "services/native_price_action.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "verification": "first_touch_only is False by default and is documented in-code as an isolated experiment, so the production path never evaluated this condition before and does not now. It gated on zone.touch_count <= 1, but touch_count increments for every bar whose range overlaps the zone and the bars forming the confirming swing already overlap it before the zone is active: measured across every zone of a structured 900-bar run the minimum was 2 and the maximum 13, never 1, so the switch could only ever yield zero proposals. With visit_count the same run gives min 1 / max 5 and the switch returns 5 proposals, a strict subset of the 33 the strategy proposes with it off. The default path produces the identical 33 proposals before and after this change, and all 33 sit on a real support or resistance level. No swing, zone, rejection, entry, stop, target or fill rule was modified. services/native_smc.py and services/mtf_policy.py still hash to their PR #6 baseline; tests/test_zone_first_visit.py pins both the unsatisfiability that motivated the change and the unchanged production count."
    },
    {
      "commits": [
        "pending (SMC paper execution permission)"
      ],
      "delta": "Add ProposedTrade.paper_execution_allowed (default True) to services/native_smc.py, and gate the SMC instance adapter on it instead of execution_allowed. This gives the SMC engine the same two-permission model the native Price Action engine already had, where paper simulation and live routing are separate grants. Requested explicitly by the repository owner after this audit established that the single-flag model was the reason no SMC order had ever been placed, in a lab or an instance.",
      "files_rehashed": [
        "services/native_smc.py"
      ],
      "alpha_rules_changed": false,
      "fill_timing_changed": false,
      "live_execution_changed": false,
      "verification": "The change adds one defaulted dataclass field and one key to public_state(); no engine method was touched. Proof that no rule moved: state_machine_hash (SetupPhase plus the AST of process_closed_bar, _transition, _advance_setups and _propose) is byte-identical at c97491396aadb44e4a972b0eccfb2deb8d7e3aa6ad7e62d2512d25ac311bdd22, and configuration_hash is unchanged at 026919f59f4cddab0c3bc4332bcc579cd94a94da6aa0c5eec9c1b0b8a881b1eb. The engine's decision output -- setups, events, FVGs, transitions and proposals, excluding only the new permission field -- hashes identically before and after at 124427d4d5c4ca6e478c4bb2551e197ce15bed2b8e530e7880d2bec0e2c52e52 over three seeds of 900 candles each, so the frozen 82-item human visual attestation still binds to exactly the sample this engine produces; EXPECTED_ENGINE_SOURCE_HASH and data/native_smc_engine_freeze_manifest.json were re-attested on that evidence and the append-only data/native_smc_visual_verification_final.json record was deliberately left at its historical hashes. Live routing is unchanged and still impossible from this path: SMCConfig.execution_allowed and the module constant EXECUTION_ALLOWED both remain False, ProposedTrade.execution_allowed still defaults False, and SMCMarketStructureEngine.__init__ still raises if either is ever set -- asserted in tests/test_native_execution_chain.py, which also drives an SMC proposal through the real adapter, signal pipeline and paper broker to a position. The freeze manifest records live_trading_enabled and forward_paper_enabled still false; only paper_trading_enabled moved to true. services/native_price_action.py, services/price_action_lab.py, services/smc_strategy_lab.py and services/mtf_policy.py still hash to their existing baseline values."
    }
  ]
}
